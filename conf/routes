# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Default path will just redirect to the computer list
GET     /                           controllers.HomeController.index()
POST    /                           controllers.HomeController.create()
GET     /showedit/:id               controllers.HomeController.showedit(id: Long)
POST    /edit/:id                   controllers.HomeController.edit(id: Long)
POST    /check/:id                  controllers.HomeController.check(id: Long)
GET     /showsearch                     controllers.HomeController.showsearch()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

